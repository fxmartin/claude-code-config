<!-- ABOUTME: Scenario for /project-init on a GitLab-master repo (issue #697). -->
<!-- ABOUTME: Checks the GitLab branch: forge file, credential helper, mirror-only github remote. -->

# Scenario: bootstrap a GitLab-master repo

You are running `/autonomous-sdlc:project-init agentic-monitor` in an empty
directory. Follow `plugins/autonomous-sdlc/skills/project-init/SKILL.md` and
`generation-rules.md`. The user answers: objective "monitor agent runs", stack
Python/FastAPI/CPython, architecture API service, private, and
**Master repo: GitLab on home-lab (default)**.

Report the exact commands you run, the files you write, and the final summary.
The `local-ci-cd` CLI is **not installed** on this machine.
