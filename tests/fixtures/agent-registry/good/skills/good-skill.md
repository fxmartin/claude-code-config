# Good Skill

This skill only references resolvable subagent types.

    Agent(subagent_type="real-agent", model="opus")
    Agent(subagent_type="general-purpose", model="sonnet")
    Agent(subagent_type=[story.agent_type], model="opus")
