{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fxmartin.me/sdlc/schemas/harness-registry.schema.json",
  "$comment": "ABOUTME: JSON-schema contract for the harness registry (harnesses.yaml, Story 20.1-001). ABOUTME: Draft 2020-12; each entry declares command template, flags, capability flags, and an output-parser id.",
  "title": "HarnessRegistry",
  "description": "Config-driven registry of agent harnesses (claude, codex, ...) the controller can dispatch to. Generalizes the dispatch seam and the adversarial-reviewers registry into one harness abstraction.",
  "type": "object",
  "additionalProperties": false,
  "required": ["harnesses"],
  "properties": {
    "default": {
      "type": "string",
      "minLength": 1,
      "description": "Name of the harness used when a role/run names none. Must be a key of 'harnesses'."
    },
    "harnesses": {
      "type": "object",
      "minProperties": 1,
      "description": "Map of harness name -> adapter declaration.",
      "additionalProperties": { "$ref": "#/$defs/harness" }
    }
  },
  "$defs": {
    "harness": {
      "type": "object",
      "additionalProperties": false,
      "required": ["command", "parser"],
      "properties": {
        "command": {
          "type": "string",
          "minLength": 1,
          "description": "Command template invoked to run an agent on this harness. May use {pr_number}/{pr_url}/{story_id} placeholders, substituted before invocation. May also carry a {model} placeholder, substituted per stage from 'models' (Story 20.7-004)."
        },
        "models": {
          "type": "object",
          "additionalProperties": { "type": "string", "minLength": 1 },
          "description": "Optional per-stage model map (stage name -> harness-specific model id) used to substitute the {model} placeholder in 'command' (Story 20.7-004). A 'default' key supplies the model for stages absent from the map. A command using {model} must declare a 'default'. Analogous to Epic-14's per-stage Balanced map, but with this harness's own model ids."
        },
        "flags": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Invocation flags appended verbatim to the rendered command."
        },
        "parser": {
          "type": "string",
          "minLength": 1,
          "description": "Id of the output parser used to interpret this harness's stdout (Story 20.1-002 registers parsers by id)."
        },
        "probe": {
          "type": "string",
          "minLength": 1,
          "description": "Optional command run in preflight to confirm the harness CLI is installed/authenticated (Story 20.5-001). A zero exit means available; omit to skip probing (status 'unknown')."
        },
        "rate_limit_probe": {
          "type": "string",
          "minLength": 1,
          "description": "Optional cheap live-API command the resume gate runs before honouring a persisted rate-limit reset epoch (issue #564). A zero exit whose output carries no rate-limit signal means the window is open, so a stale epoch is cleared; anything else keeps the run parked. Omit to skip the re-probe."
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether this harness may be dispatched to. Defaults to true."
        },
        "capabilities": {
          "type": "object",
          "additionalProperties": { "type": "boolean" },
          "description": "Capability flags (worktree_isolation, parallel, json_contract, usage_tracking, rate_limit_aware, ...) the controller gates run modes on (Feature 20.5)."
        },
        "metered": {
          "type": "boolean",
          "description": "Whether this harness's cost is a real/notional $/Mtok figure (true, default) or has no per-token price at all — a local-inference harness with no metered API (Story 31.2-003). false without 'local_rate_usd_per_million_tokens' records cost as 'not metered', never $0."
        },
        "local_rate_usd_per_million_tokens": {
          "type": "number",
          "minimum": 0,
          "description": "An explicit, recorded $/Mtok-equivalent rate for a non-metered harness (e.g. an assumed energy cost for local inference). Only consulted when 'metered' is false; ignored otherwise. Unset (default) leaves a non-metered harness's cost as 'not metered' rather than inventing a number (Story 31.2-003)."
        }
      }
    }
  }
}
